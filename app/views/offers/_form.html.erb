<%= form_for(@offer) do |f| %>
  <% if @offer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@offer.errors.count, "error") %> prohibited this offer from being saved:</h2>

      <ul>
      <% @offer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :location %><br />
    <%= f.text_field :location %>
  </div>
  <div class="field">
    <%= f.label :date %><br />
    <%= f.date_select :date %>
  </div>
  <div class="field">
    <%= f.label :responsible %><br />
    <%= f.text_field :responsible %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<p/>
<p>
  <%= render "products/list" %>
</p>

<% unless @offer.new_record? %>
  <%= form_tag(add_product_path(:id => @offer.id)) do %>
    <div class="field">
      <%= label_tag(:product, "Add a product:") %>
      <%= select_tag(:product, options_from_collection_for_select(Product.all, :id, :name)) %>
      <%= submit_tag("Add", :name => "add_product") %>
    </div>
  <% end %>
<% end %>

